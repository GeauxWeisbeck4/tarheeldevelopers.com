<div class="grid grid-cols-6 gap-4">
  {% for article in topArticle %}
  <div class="col-span-4 px-10 md:w-100 w-140 mr-10 mt-10">
    <div class="h-full border-2 border-gray-800 rounded-lg overflow-hidden">
    <a href="{{ article.url }}" class="block hover:bg-gray-900">
      <img src="{{ article.image | default('/images/default.jpg') }}" alt="blog" class="lg:h-48 md:h-36 w-full object-cover object-center" />
      <div class="p-6">
        <h2 class="title-font text-lg font-medium text-white mb-3">{{ article.title }}</h2>
        <p class="leading-relaxed mb-3 text-gray-400">{{ article.excerpt }}
      </div>
      </a>
    </div>
  </div>
  {% endfor %}
  <div class="col-span-2 w-full">
    <h1 class="pr-20 text-2xl pt-20 pb-5">Read the blog!</h1>
    <p class="pr-20 pb-10">Check out our blog over here so you can read all about this or that and that or this. We can get with this or you can get with that it's a heart attack.</p>
    <a href="/blog" class="bg-green-400 hover:bg-blue-400 text-white font-semibold px-8 py-4 border-solid-white border-2 rounded-xl">Read the Blog!</a>
  </div>
  {% for article in featuredArticles %}
  <div class="ml-10 col-span-2">
  <div class="p-4">
    <div class="h-100 w-100 border-2 border-gray-800 rounded-lg overflow-hidden">
    <a href="{{ article.url }}" class="block hover:bg-gray-900">
      <img class="lg:h-48 md:h-36 w-full object-cover object-center" src="{{ article.image | default('/images/default.jpg') }}" alt="blog" />
      <div class="p-6">
        <h2 class="title-font text-lg font-medium text-white mb-3">{{ article.title }}</h2>
        <p class="leading-relaxed mb-3 text-gray-400">{{ article.excerpt }}</p>
      </div>
      </a>
    </div>
  </div>
  </div>
{% endfor %}
</div>